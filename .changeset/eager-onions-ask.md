---
"takumi": minor
---

Support `textWrap`, `textWrapMode`, `whiteSpace`, `whiteSpaceCollapse` properties

**BREAKING CHANGE: by default text will collapse instead of preserve**, use `whiteSpace: pre;` to get the same behavior
